import { ExtractRecordValuePipe } from './extract-record-value.pipe';

describe('ExtractRecordValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractRecordValuePipe();
    expect(pipe).toBeTruthy();
  });
});
